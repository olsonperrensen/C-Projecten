#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#include <string.h>
#include <stdio.h>
#include <math.h>
#define LEN 255
//typedef enum {Mon,Tue} D;
//typedef struct { int age, ID; }P;
int main() {
    //P myP = { 0, 0 };
    //FILE* fp = fopen("a.txt","w");
  /*  int control, counter = 0;
    while (control = fwrite(&myP,sizeof(P),1,fp))
    {
        if (counter++, counter > 50)
        {
            break;
        }
    }*/
    //P* pp = &myP.ID;
    //P* p = &myP;
    //P* ppp = &p->ID;
  /*  char str[] = "BEST WAY NOT TO USE THE SPACES LOL";
    char* piece = strtok(str, " "); while (piece) { printf("%s\n", piece); piece = strtok(NULL, " "); }*/


    //P myP;
    //P* p0 = &(myP.ID);
    //P* p1 = &myP.ID;
    //int i = M_PI;
    /*char str[LEN];
    strcpy(str,"This is a string");
    char* piece;
    piece = strtok(str, " ");
    while (piece) { 
        printf("%s\n", piece); 
        piece = strtok(NULL, " "); 
    }*/
   /* D myD; 
    int temp;
    scanf("%d%*c",&temp);
    myD = (D)temp;
    switch (myD)
    {
    case 0:
        printf("You're on a Monday");
        break;
    case 1:
        printf("You're on a Tuesday");
        break;
    default:
        break;
    }*/
    //char* p = (char*)malloc(sizeof("Hi there"));
    //FILE* fp = fopen("b.txt","r+");
    ////strcat(str,"\0");
    //fwrite(p, sizeof(p), 1, fp);
    //fflush(fp);
    //fclose(fp);
    return(0);
}
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define SIZE 46

int main(void)
{
	int myarray[SIZE];
	for (int i = 0; i < SIZE; i++)
	{
		//myarray[i] = i+1;	printf(" %d", myarray[i]);
		i[myarray] = i+1;	printf(" %d", myarray[i]);

	}

}